void BubbleSort(int *A, int n){
	int j, i, tmp;

	for(i = 0; i < n - 1; ++i)
		for(int j = 0; j < n - 1 - i; ++j)
			if(A[j] > A[j + 1]){
				//swap
				tmp = A[j];
				A[j] = A[j + 1];
				A[j + 1] = tmp;

			}
}
